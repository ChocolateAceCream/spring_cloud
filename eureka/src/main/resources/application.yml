spring:
  application:
    name: eureka-server
  boot:
    admin:
      url: https://192.168.1.15:8079
      username: admin
      password: password

server:
  port: 8090

eureka:
  client:
    registerWithEureka: false
    fetchRegistry: false

management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: ALWAYS